syntax="proto3";
package responses;
option go_package = "./internal/responses;responses";

import "common.proto";

message Generic {
    ResponseStatus status = 1;
    uint32 alignment = 2; 
    repeated string messages = 3;
    string command = 4;
    bool ascii = 5;
}
